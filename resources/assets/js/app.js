var Dropdown = require('./basscss/dropdown');
var Fuellog  = require('./app/fuellog');

/**
 * This is probably not the best way to call modules through inline scripts,
 * but it does work for now.
 *
 */
global.Fuellog = Fuellog;